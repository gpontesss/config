[user]
	email = pontes.guisilva@gmail.com
	name = gpontesss
	signinkey = 474255977B1F704C

[alias]
	adog = log --all --decorate --oneline --graph
	dog = log --decorate --oneline --graph
	s = status -s
	curb = branch --show-current
	c = commit -v
	ca = commit -v --amend
	ck = ! git checkout "$(git branch | grep -v '*' | fzf | xargs)"

[merge]
	tool = vimdiff

[core]
	editor = nvim

[commit]
	gpgsign = false

[gpg]
	program = gpg

[url "git@github.com:tembici/"]
	insteadOf = tembici:

[url "git@github.com:"]
	insteadOf = gh:
[pull]
	ff = only
